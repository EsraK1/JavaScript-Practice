const names = ["rizvan", "hatice", "muhammed"];

console.log(names[1]);
console.log(names.length);

const new_names = names.concat("Esra");

console.log(new_names);

console.log(names.concat("esra"));

names.forEach((names) => {
  console.log(names);
});
